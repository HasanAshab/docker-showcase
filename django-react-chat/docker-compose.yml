services:
  client:
    build:
      context: client
      target: dev
    env_file: ./client/.env.dev
    ports:
      - 5000:5000
    stdin_open: true
    volumes:
      - ./client:/app
      - /app/node_modules
    networks:
      - client-server
    depends_on:
      - server

  server:
    build:
      context: server
      target: dev
    command: python manage.py runserver 0.0.0.0:8000
    ports:
      - 8000:8000
    volumes:
      - ./server:/app
    networks:
      - client-server
      - server-db
    depends_on:
      - db

  db:
    image: postgres:14
    environment:
      POSTGRES_DB: ranker
      POSTGRES_USER: ranker_user
      POSTGRES_PASSWORD: ranker_pass
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - server-db

networks:
  client-server:
  server-db:

volumes:
  db-data: